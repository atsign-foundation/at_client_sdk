{
  "assertion_results": [
    {
      "text": "Shows getDescendant() call",
      "passed": true,
      "evidence": "\"Call `collection.getDescendant(...)` on the **root-level collection**\" and code snippet: `final comment = await collection.getDescendant<Comment>(ancestry: event.ancestry, id: event.id, owner: event.owner, ...)`"
    },
    {
      "text": "Uses ancestry: event.ancestry and id: event.id from the event",
      "passed": true,
      "evidence": "Code snippet lines: `ancestry: event.ancestry,        // root-to-direct-parent chain from the event` and `id: event.id,                    // the comment's id`"
    },
    {
      "text": "Mentions that ancestry.last is the direct parent (root-first ordering)",
      "passed": true,
      "evidence": "\"`ancestry` is ordered **root-first**: `ancestry[0]` is the outermost ancestor (e.g. the post), and `ancestry.last` is the direct parent of the comment.\""
    },
    {
      "text": "Warns that getDescendant throws if CAncestor.owner is null",
      "passed": false,
      "evidence": "The response mentions CAncestor.owner is null in CSubItemDeleted events and that getDescendant 'cannot be called' in that case, but never states that getDescendant **throws** an exception. It only describes the null-return behavior: 'getDescendant returns null if any item in the ancestor chain has expired or is unavailable.'"
    }
  ],
  "summary": {
    "passed": 3,
    "failed": 1,
    "total": 4,
    "pass_rate": 0.75
  }
}
