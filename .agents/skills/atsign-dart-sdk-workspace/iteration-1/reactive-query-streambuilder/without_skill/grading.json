{
  "assertion_results": [
    {
      "text": "Shows .query().watch() for reactive results",
      "passed": false,
      "evidence": "The response uses `.watch()` only in the Drift example as `(select(todos)..where()..orderBy()).watch()`. The `.query().watch()` pattern specific to the atSign SDK is not present anywhere in the response."
    },
    {
      "text": "Includes .where() clause for filtering done=false",
      "passed": true,
      "evidence": "Multiple examples include filtering: Firestore uses `.where('done', isEqualTo: false)`, Drift uses `.where((t) => t.done.equals(false))`, and the custom option uses `.where((t) => !t.done)`."
    },
    {
      "text": "Includes .orderBy() for sorting by due date",
      "passed": true,
      "evidence": "Firestore example uses `.orderBy('dueDate')` and Drift uses `.orderBy([(t) => OrderingTerm(expression: t.dueDate, ...)])`. The summary table also shows `.orderBy('dueDate')` and `.orderBy([...])` for the respective backends."
    },
    {
      "text": "Mentions that queries run on-device (not server-side)",
      "passed": false,
      "evidence": "The response explicitly notes Firestore supports 'server-side filtering and ordering natively' for Option 1. There is no mention that queries run on-device as a distinguishing feature of an SDK; the response is generic Flutter and does not address on-device query execution in the context of the atSign SDK."
    }
  ],
  "summary": {
    "passed": 2,
    "failed": 2,
    "total": 4,
    "pass_rate": 0.5
  }
}
