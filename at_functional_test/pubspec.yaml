name: at_functional_test
publish_to: none
description: Functional tests for the at_client_sdk packages
version: 1.0.0

environment:
  sdk: '>=2.14.4 <3.0.0'

dependencies:
  at_client:
    path: ../at_client

dev_dependencies:
  test: ^1.21.4
  lints: ^2.0.0
  at_demo_data: ^1.0.0
  coverage: ^1.5.0
