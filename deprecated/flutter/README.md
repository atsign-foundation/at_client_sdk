# Deprecated Flutter packages

This directory holds tombstones for Flutter packages that used to live under
`packages/` and are no longer maintained as packages.

These tombstones are plain documentation. Do not add `pubspec.yaml`, `lib/`,
`example/`, tests, or platform folders here.

