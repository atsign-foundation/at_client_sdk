name: at_onboarding_cli_functional_tests
description: functional tests for at_onboarding_cli.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  at_lookup: ^3.4.2
  at_client: ^3.10.0
  at_commons: ^5.5.0
  at_demo_data: ^1.2.0

dev_dependencies:
  elliptic: ^0.3.10 # test-only: secp256r1 keys for the secure-element mock
  lints: ^6.0.0
  test: ^1.25.0
