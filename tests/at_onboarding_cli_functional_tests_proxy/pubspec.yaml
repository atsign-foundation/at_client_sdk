name: at_onboarding_cli_functional_tests_proxy
version: 1.0.0
description: Functional tests for at_onboarding_cli with proxy support

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  at_demo_data: ^1.2.0

dev_dependencies:
  test: ^1.21.0