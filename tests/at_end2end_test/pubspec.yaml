name: at_end2end_test
description: A starting point for Dart libraries or applications.
version: 1.0.0
homepage: https://www.atsign.com

publish_to: none

environment:
  sdk: '>=2.15.0 <4.0.0'

dependencies:
  at_demo_data: ^1.0.1
  at_client:
    path: ../../packages/at_client

dependency_overrides:
  at_client:
    git:
        url: https://github.com/atsign-foundation/at_client_sdk.git
        path: packages/at_client
        ref: replace_encryption_methods_with_atchops
  watcher: ^1.1.0

dev_dependencies:
  test: ^1.24.9
  lints: ^2.0.0
  coverage: ^1.5.0
