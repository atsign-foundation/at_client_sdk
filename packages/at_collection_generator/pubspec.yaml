name: at_collection_generator
description: A package containing generator for at_collection
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  build: ^2.3.1
  source_gen: ^1.2.6
  at_collection_annotation:
    path: ../at_collection_annotation/

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
