name: at_telemetry
description: Generic telemetry models and contracts for applications and atServers
version: 0.1.0
repository: https://github.com/atsign-foundation/at_client_sdk/tree/trunk/packages/at_telemetry

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  dartastic_opentelemetry: ^0.11.0
  fixnum: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
