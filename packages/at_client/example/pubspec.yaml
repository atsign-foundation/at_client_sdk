name: at_client_example
description: An example application to demonstrate the at_client package usage.
version: 1.0.0

environment:
  sdk: ">=2.15.0 <4.0.0"

dependencies:
  at_client: ^3.7.0

dev_dependencies:
  lints: ^1.0.0
